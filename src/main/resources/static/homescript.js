function abrirPagina(pagina) {
    alert('Redirecionando para a página de ' + pagina.toUpperCase() + '.');
    // No Spring Boot, redirecionar o usuário assim:
    // window.location.href = '/' + pagina;
}